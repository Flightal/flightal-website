---
---

<section class="privacy section" id="privacy">
  <div class="container-narrow">
    <header class="privacy-head reveal">
      <h2 class="section-title">
        Pilots trust Flightal because <span class="italic-emphasis">pilots designed Flightal.</span>
      </h2>
    </header>

    <div class="prose privacy-body reveal">
      <p>
        On-device inference means cockpit conversations stay in the cockpit. We
        capture that a discussion happened, not what was said. Operators get
        aggregated insights and flagged events, not raw recordings.
      </p>
      <p class="privacy-emphasis">
        This isn't surveillance. This is operational visibility that pilots are
        comfortable flying with.
      </p>
    </div>
  </div>
</section>

<style>
  .privacy-head {
    margin-bottom: clamp(2.5rem, 5vw, 3.5rem);
  }
  .privacy-head .section-title {
    max-width: none;
  }
  .privacy-emphasis {
    font-family: var(--font-display);
    font-weight: 500;
    font-style: italic;
    font-size: clamp(1.25rem, 1.05rem + 1vw, 1.875rem);
    line-height: 1.25;
    color: var(--ink-strong);
    margin-top: 2rem !important;
  }
</style>
